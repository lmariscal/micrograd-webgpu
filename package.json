{
  "name": "micrograd-wgpu",
  "module": "src/main.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.40"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "cp -r src/static/* out/ && bunx esbuild src/*.ts --outdir=out/js --bundle --platform=browser --format=esm --sourcemap --serve=3000 --servedir=out"
  }
}
